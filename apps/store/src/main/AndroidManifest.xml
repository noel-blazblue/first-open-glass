<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <permission
        android:name="com.glass.dining.STORE_CATALOG"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.glass.dining.STORE_CATALOG" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:name=".StoreApp"
        android:allowBackup="false"
        android:icon="@android:drawable/ic_menu_mylocation"
        android:label="门店录入"
        android:supportsRtl="true"
        android:theme="@style/Theme.StoreApp"
        tools:replace="android:allowBackup">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <provider
            android:name=".StoreCatalogProvider"
            android:authorities="com.glass.dining.store.catalog"
            android:exported="true"
            android:permission="com.glass.dining.STORE_CATALOG" />
    </application>
</manifest>
