---
description: 通用 Agent 反过拟合：需求示例不得变成生产本体
alwaysApply: true
---

# 通用 Agent 设计

这是通用型智能 Agent。需求里的场景示例默认只用于验收测试，不能直接写成生产主路径。

## 禁止把示例做成世界模型

- 不得把需求示例变成生产枚举、关键词词典、`if` / `when` 分支或固定摘要。
- 通用事件、观察、记忆、提示词必须能接收从未见过的活动，不必为新活动改代码。
- 咖啡、工位、会议室、打印、取快递等只属于测试 fixture 或产品文案，不属于通用事件本体。

## 代码与模型的分工

- 确定性代码只处理协议与不变量：schema、证据等级、时间单调、队列上限、生命周期、失败回退。
- 开放世界语义（人物、物体、动作、地点、意图、活动名称）交给模型或可扩展 provider。
- 模型输出是提案；写入状态前必须经过纯逻辑 reducer。失败时保守保存本次观察，不臆造意图。

## 领域特化必须隔离

- 确需领域能力时，隔离为可插拔模块（例如 `shared/indoor` 的走廊/电梯拓扑）。
- 领域字段只能作为可选证据进入通用事件，不能成为通用分类或转移条件。
- 导航目的地、探索状态和用户生活事件分层存储，用 episode / pose / node id 关联，不要互相覆盖。

## 提交前自检

- 搜索示例词（如 coffee / meeting / desk / 咖啡 / 工位）是否进入通用 tracker、reducer、prompt。
- 至少用两个需求之外的新场景验证：只靠模型提案即可运行，生产代码无需新增分支。
